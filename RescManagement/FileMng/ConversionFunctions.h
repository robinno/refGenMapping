#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "../../TYPES.h"

/**
 * Transforms the given character to fit in the "BASE" type.
 */
BASE charToBase(char c);

/**
 * Transforms the given BASE type to a character.
 */
char baseToChar(BASE base);

#endif
